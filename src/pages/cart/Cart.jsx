import React from "react";
import './Cart.css'
import { CartContainer } from "./CartContainer";

export const Cart = () => {
    return (
        <div className="cartContainer">
            <CartContainer/>
        </div>
    )
}